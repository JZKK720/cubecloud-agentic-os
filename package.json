{
  "name": "cubecloud-agentic-os",
  "private": true,
  "version": "2.10.68",
  "description": "Desktop-first agentic operating system shell for Cubecloud",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "cd agent-desktop && npm run dev",
    "build": "npm run build --workspace @cubecloud/platform-core && cd agent-desktop && npm run build",
    "typecheck": "npm run typecheck --workspace @cubecloud/platform-core && cd agent-desktop && npm run typecheck",
    "graphify:repo": "node scripts/graphify-repo.cjs run",
    "graphify:update": "node scripts/graphify-repo.cjs update",
    "graphify:query": "node scripts/graphify-repo.cjs query"
  },
  "devDependencies": {
    "electron": "^42.4.1"
  }
}
