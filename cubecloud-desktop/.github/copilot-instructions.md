# Copilot Instructions

Follow the repo policy in `AGENTS.md`.

Default working rules for this repo:

- Use `karpathy-guidelines` for non-trivial work.
- Use `design-taste-frontend` for renderer and UI work.
- Build and implement in `cubecloud-desktop`; treat upstream `hermes-desktop` as reference-only unless the user explicitly asks otherwise.