# V2.10.36 - inner doc retirement (Option S) — commit handoff

> **Generated:** 2026-06-08
> **Source tree:** d:\users\joeyzh\github-pr\cubecloud-agentic-os (Local Build)
> **Git state at handoff:** working tree contains all V2.10.36 edits, but the directory is **not** a git repository — there is no .git at the root or at the parent. The commit must be made in the canonical cubecloud-agentic-os clone, not here.

### Update 2026-06-08 (post-autopilot attempt)

After the original handoff was written, the user requested a **direct push to a fresh private GitHub repo at `https://github.com/JZKK720/cubecloud-agentic-os`**, treating this `Local Build` as the canonical source. The new repo has been created (verified at 2026-06-08, empty placeholder, 200 OK, no files, no commits). The push is **paused** because the terminal session used for the secret scan went unresponsive mid-run.

**Verified pre-push state (as of 2026-06-08):**

| Check | Result | Probe |
|---|---|---|
| `https://github.com/JZKK720/cubecloud-agentic-os` exists | ✅ 200, empty placeholder, no files, no commits | `probe_after_create.txt` |
| Stale URLs `cubecloud-agent-desktop` and `https-github.com-JZKK720-cubecloud-agent-desktop` are 404 | ✅ confirmed | `probe_after_create.txt` |
| SSH key `id_ed25519` is registered on GitHub as `JZKK720` | ✅ "Hi JZKK720! You've successfully authenticated" | `probe_ssh.txt` |
| HTTPS credential cache present for `git:https://github.com` | ✅ `LegacyGeneric:target=git:https://github.com` in `cmdkey /list` | `probe_creds.txt` |
| `git config --global user.name` and `user.email` | ✅ `JZKK720` / `joeyzh@live.com` | `probe_creds.txt` |
| `.gitignore` at the working tree root | ✅ existing, minimal-but-correct for the monorepo (includes `node_modules/`, `dist/`, `.review-extras/`, etc.) | direct file read |
| Secret scan | ❓ **incomplete** — output file was never readable; terminal session went unresponsive during a follow-up smoke test | — |

**The push is paused, not failed.** The local tree is unchanged. The GitHub repo is empty and ready to receive a push. The auth path is verified. The remaining work is: (a) run the secret scan in a fresh terminal session, (b) show the staged-set preview, (c) install the pre-push hook, (d) `git init -b main`, `git add -A`, `git commit`, `git remote add origin`, `git push -u origin main`. None of these need a fresh canonical clone; they happen in this `Local Build` tree.

---

## TL;DR for the committer

1. Apply the diff below in the canonical cubecloud-agentic-os clone (the one with the GitHub origin).
2. Run the 4 validation commands in [§ Validation](#validation). All four are green as of handoff time.
3. git add -A && git commit with the message in [§ Commit message](#commit-message).
4. Open a PR against main titled V2.10.36 - inner doc retirement (Option S).

---

## Files changed

### Deleted (4)

| Path | Bytes |
|---|---|
| cubecloud-desktop/docs/COMMIT-1-2-APPLIED.md | 7,325 |
| cubecloud-desktop/docs/COMMIT-3-9-APPLIED.md | 16,293 |
| cubecloud-desktop/docs/V2-COMMIT-PLAN.md | 16,441 |
| cubecloud-desktop/docs/CODEGRAPH_WORKSPACE_MIGRATION.md | 2,105 |

### Archived (2, moved into the new cubecloud-desktop/docs/superpowers/archive/ subdir)

| From | To | Bytes |
|---|---|---|
| cubecloud-desktop/docs/superpowers/plans/2026-04-30-windows-winget-fedora-rpm-release.md | cubecloud-desktop/docs/superpowers/archive/2026-04-30-windows-winget-fedora-rpm-release.md | 39,227 |
| cubecloud-desktop/docs/superpowers/specs/2026-04-30-windows-winget-fedora-rpm-release-design.md | cubecloud-desktop/docs/superpowers/archive/2026-04-30-windows-winget-fedora-rpm-release-design.md | 15,682 |

### New (1) — self-describing archive subdir README

- cubecloud-desktop/docs/superpowers/archive/README.md"
Add-Line "
Add-Line 

- cubecloud-desktop/docs/superpowers/plans/"
Add-Line 

### Modified (6)

| Path | Change |
|---|---|
| BRANDING_AND_LICENSE.md | Appended ## V2.10.36 - inner doc retirement (Option S) (Scope / What changed / Why this is the right V2.10.36 step / Out of scope sections) |
| README.i18n.md | Policy header bumped from V2.10.35 to V2.10.36 |
| docs/HANDBOOK.md (hardlink to cubecloud-desktop/docs/HANDBOOK.md) | Dropped 3 deleted-file rows from the Where to look next table; repointed 3 superpowers/specs references to docs/superpowers/archive/2026-04-30-windows-winget-fedora-rpm-release-design.md; rewrote the line-250 release-process blurb and the line-299 attribution note |
| docs/RETIRED_AND_LEGACY.md | Added the Inner doc retirement (V2.10.36 Option S) row |
| docs/handbook/README.md (hardlink to cubecloud-desktop/docs/handbook/README.md) | Same 5 edits as the master HANDBOOK, plus an attribution-note update |
| cubecloud-desktop/changelogs/0.6.0.md (line 349) | The lone Full plan in [docs/V2-COMMIT-PLAN.md] reference is rewritten to a V2.10.36 retirement note with a forward pointer to BRANDING_AND_LICENSE.md §V2.10.36 |

### Regenerated (1)

- docs/Cubecloud-README-en-zh.pdf — re-rendered to bake the V2.10.35 conceptual-model sub-section and the V2.10.36 lifecycle bump.

---

## Commit message

```
V2.10.36 - inner doc retirement (Option S)

Scope: cubecloud-desktop/docs/COMMIT-{1-2,3-9}-APPLIED.md,
       cubecloud-desktop/docs/V2-COMMIT-PLAN.md,
       cubecloud-desktop/docs/CODEGRAPH_WORKSPACE_MIGRATION.md,
       cubecloud-desktop/docs/superpowers/{plans,specs}/2026-04-30-windows-winget-fedora-rpm-release{,-design}.md,
       plus the now-stale references in docs/HANDBOOK.md,
       docs/handbook/README.md, and cubecloud-desktop/changelogs/0.6.0.md.

Rationale: A pre-retirement Python word-boundary scan over 804 source/
doc/config files confirmed all 80+ hits for the 6 candidate files were
bare-filename mentions in "Where to look next" lists. None were linked
from code, tests, release workflows, or .gitignore patterns. The 4
commit-log / workspace-migration files are deleted outright. The 2
release-design files are archived (not deleted) into the new
cubecloud-desktop/docs/superpowers/archive/ subdir so the BRANDING
history still resolves to a real artifact.

Stale references in HANDBOOK.md, handbook/README.md, and the 0.6.0
changelog are repointed to the archive/ subdir or to BRANDING_AND_LICENSE.md
§V2.10.36. The README.i18n.md policy header is bumped from V2.10.35 to
V2.10.36. The combined README PDF is re-rendered.

Validation: prelaunchSeed 40/40, vitest 27/27 (i18n 13 + headroom-footer
9 + winget 5), desktop typecheck (node + web) clean, PDF re-render OK.
```

---

## Validation

| Command | Result |
|---|---|
| 
ode apps/desktop-shell/prelaunchSeed.smoke.mjs | 40/40 pass |
| 
pm run typecheck (in cubecloud-desktop/) | clean (node + web) |
| 
pm run test (in cubecloud-desktop/) | 27/27 pass (i18n 13 + headroom-footer 9 + winget 5) |
| 
ode scripts/v2.10.20-readme-combined-pdf.cjs | 812,792 bytes written |

---

## Git state at handoff


The working tree at d:\users\joeyzh\github-pr\cubecloud-agentic-os is **not** a git
repository. There is no .git directory at the root, no .git at the parent
(d:\users\joeyzh\github-pr\), and therefore no origin, no main, and no PR target.

**Do not run git init in this tree.** It would create a brand-new repo with no
- ~\projects\cubecloud-agentic-os\"
Add-Line 
- A WSL path (\\wsl$\Ubuntu\home\<user>\cubecloud-agentic-os\)
- A different drive (e.g. d:\code\cubecloud-agentic-os\, e:\repos\cubecloud-agentic-os\)

Run git remote -v in each candidate. The one with github.com as the remote
is the canonical clone.

### If the canonical clone is on a different machine

Either: (a) sync this Local Build to the canonical clone via your existing
file-sync / patch workflow and commit there, or (b) ask the agent on the
canonical clone to apply the diff in this handoff document.

### To apply the diff

The cleanest approach in the canonical clone is to re-run the same edits
using the same scripts (they are idempotent — they assert exact-match

## Update 2026-06-09 (V2.10.43c deferred)

**V2.10.43a + V2.10.43b are committed and pushed** (commits 3490df0 and 7d21aa6). The blank-window bug is fixed and the rebuild at d:\temp-cubecloud-build\dist has been verified end-to-end (window title Agent Desktop, processes spawn, i18next initializes).

**V2.10.43c (directory rename cubecloud-desktop/ -> agent-desktop/) is DEFERRED.** Scope: 200+ path references across 30+ files (docs, scripts, workflows, BRANDING history, Windows hardlinks, inner .github/workflows/release.yml path). All user-facing artifacts (npm package, exe, updater cache, productName, GitHub assets) are already aligned from V2.10.43b; the directory name is purely an internal source-tree path with zero user impact. The high-edit-cost rename deserves its own dedicated session with a fresh look at the hardlink assumptions in docs/sync-docs.ps1. Will be tracked as V2.10.44+ candidate.

**V2.10.43d is committed and pushed** (commit a7e117d). v0.6.1 is **PUBLISHED** at https://github.com/JZKK720/cubecloud-agentic-os/releases/tag/v0.6.1 with 4 assets (setup.exe, portable.exe, blockmap, latest.yml), all SHA-256 verified, all download URLs HTTP 200. v0.6.0 marked as pre-release (superseded). Build path: d:\v0.6.1-build\dist (redirected via --config.directories.output to bypass the Windows Defender file lock on the standard dist).

**Cleanup**: d:\temp-cubecloud-build\ (1.7 GB) and d:\v0.6.1-build\ (1.7 GB) are no longer needed for the release. Safe to remove via `Remove-Item d:\temp-cubecloud-build,d:\v0.6.1-build -Recurse -Force`. The canonical cubecloud-desktop\dist\ is still locked by Windows Defender — leave it alone until Defender releases the file handle (may take hours).
